server:
  port: 8761

eureka:
  client:
    # colocamos false porque no es un cliente
    fetch-registry: false
    register-with-eureka: false
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
